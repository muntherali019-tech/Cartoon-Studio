{
  "name": "cartoon-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cartoon Studio — an AI studio that turns a prompt or a photo into cartoons, comic strips, avatars and sticker packs, then exports them.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "express": "^4.19.2",
    "pg": "^8.13.1"
  }
}
