import React from "react";
import ReactDOM from "react-dom/client";
import CartoonStudio from "./CartoonStudio.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CartoonStudio />
  </React.StrictMode>
);
